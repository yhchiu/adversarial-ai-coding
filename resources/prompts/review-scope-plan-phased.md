{{PLAN_FILE}} compared with {{SPEC_FILE}}: feasibility, test coverage, whether tasks are small and independent, and whether checkbox format is correct. The plan must be split into "## Phase N: <title>" sections. Treat as blockers: a phase that is a horizontal technical layer instead of a vertical functional slice (for example a standalone "frontend", "backend", or "database" phase instead of a capability slice like "user can log in" that spans every layer), a phase without an "Acceptance:" line describing observable behavior at a stable boundary, a phase without "- [ ] " tasks, and "- [ ] " tasks outside any phase.
